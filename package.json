{
  "scripts": {
    "build": "tsc && uglifyjs dist/dks.js > dist/dks.min.js && node-sass src/scss -o dist --output-style compressed"
  },
  "devDependencies": {
    "node-sass": "^6.0.1",
    "typescript": "^4.3.5",
    "uglify-js": "^3.14.1"
  }
}
